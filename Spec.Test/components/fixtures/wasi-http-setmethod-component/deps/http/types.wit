package wasi:http@0.2.8;
interface types {
    variant method {
        get,
        head,
        post,
        put,
        delete,
        connect,
        options,
        trace,
        patch,
        other(string),
    }
    resource outgoing-request {
        set-method: func(method: method) -> result;
    }
}
