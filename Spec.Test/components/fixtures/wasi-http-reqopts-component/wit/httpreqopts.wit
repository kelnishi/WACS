package local:httpreqopts;
world demo {
    import wasi:http/types@0.2.3;
    /// Returns option-disc as u32.
    export ask-disc: func(handle: u32) -> u32;
    /// Returns the u64 timeout payload at retArea+8.
    export ask-timeout: func(handle: u32) -> u64;
}
