package local:fsreaddirect;
world demo {
    import wasi:filesystem/types@0.2.8;
    /// Calls descriptor.read(handle, 8, 0); returns
    /// (length << 1) | (eof_flag) — packs the byte count
    /// and EOF bit into a single u32.
    export ask-read: func(handle: u32) -> u32;
}
