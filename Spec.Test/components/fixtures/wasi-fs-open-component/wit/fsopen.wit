package local:fsopen;
world demo {
    import wasi:filesystem/types@0.2.8;
    /// open-at(handle, "child", flags) — uses Create open-flag,
    /// returns the new descriptor handle.
    export ask-open: func(handle: u32) -> u32;
}
