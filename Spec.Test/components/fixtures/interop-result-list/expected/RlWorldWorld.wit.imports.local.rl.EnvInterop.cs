// Generated by `wit-bindgen` 0.30.0. DO NOT EDIT!
// <auto-generated />
#nullable enable

using System;
using System.Runtime.CompilerServices;
using System.Collections;
using System.Runtime.InteropServices;
using System.Text;
using System.Collections.Generic;
using System.Diagnostics;
using System.Diagnostics.CodeAnalysis;

namespace RlWorldWorld.wit.imports.local.rl
{
    public static class EnvInterop {

        internal static class ReadWasmInterop
        {
            [DllImport("local:rl/env", EntryPoint = "read"), WasmImportLinkage]
            internal static extern void wasmImportRead(long p0, nint p1);

        }

        public  static unsafe byte[] Read(ulong n)
        {

            var retArea = new uint[3];
            fixed (uint* retAreaByte0 = &retArea[0])
            {
                var ptr = (nint)retAreaByte0;
                ReadWasmInterop.wasmImportRead(unchecked((long)(n)), ptr);

                Result<byte[], global::RlWorldWorld.wit.imports.local.rl.IEnv.ErrKind> liftedResult;

                switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                    case 0: {

                        var array = new byte[BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4))];
                        new Span<byte>((void*)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 4), 4))), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4))).CopyTo(new Span<byte>(array));

                        liftedResult = Result<byte[], global::RlWorldWorld.wit.imports.local.rl.IEnv.ErrKind>.ok(array);
                        break;
                    }
                    case 1: {

                        global::RlWorldWorld.wit.imports.local.rl.IEnv.ErrKind lifted;

                        switch (new Span<byte>((void*)(ptr + 4), 1)[0]) {
                            case 0: {

                                lifted = global::RlWorldWorld.wit.imports.local.rl.IEnv.ErrKind.notFound();
                                break;
                            }
                            case 1: {

                                lifted = global::RlWorldWorld.wit.imports.local.rl.IEnv.ErrKind.denied(unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)))));
                                break;
                            }

                            default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 4), 1)[0]}");
                        }

                        liftedResult = Result<byte[], global::RlWorldWorld.wit.imports.local.rl.IEnv.ErrKind>.err(lifted);
                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
                }
                if (liftedResult.IsOk) {
                    var tmp = liftedResult.AsOk;
                    return tmp;
                } else {
                    throw new WitException(liftedResult.AsErr!, 0);
                }
            }

            //TODO: free alloc handle (interopString) if exists
        }

        internal static class ReadU32WasmInterop
        {
            [DllImport("local:rl/env", EntryPoint = "read-u32"), WasmImportLinkage]
            internal static extern void wasmImportReadU32(long p0, nint p1);

        }

        public  static unsafe uint[] ReadU32(ulong n)
        {

            var retArea = new uint[3];
            fixed (uint* retAreaByte0 = &retArea[0])
            {
                var ptr = (nint)retAreaByte0;
                ReadU32WasmInterop.wasmImportReadU32(unchecked((long)(n)), ptr);

                Result<uint[], global::RlWorldWorld.wit.imports.local.rl.IEnv.ErrKind> liftedResult;

                switch (new Span<byte>((void*)(ptr + 0), 1)[0]) {
                    case 0: {

                        var array = new uint[BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4))];
                        new Span<uint>((void*)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 4), 4))), BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4))).CopyTo(new Span<uint>(array));

                        liftedResult = Result<uint[], global::RlWorldWorld.wit.imports.local.rl.IEnv.ErrKind>.ok(array);
                        break;
                    }
                    case 1: {

                        global::RlWorldWorld.wit.imports.local.rl.IEnv.ErrKind lifted;

                        switch (new Span<byte>((void*)(ptr + 4), 1)[0]) {
                            case 0: {

                                lifted = global::RlWorldWorld.wit.imports.local.rl.IEnv.ErrKind.notFound();
                                break;
                            }
                            case 1: {

                                lifted = global::RlWorldWorld.wit.imports.local.rl.IEnv.ErrKind.denied(unchecked((uint)(BitConverter.ToInt32(new Span<byte>((void*)(ptr + 8), 4)))));
                                break;
                            }

                            default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 4), 1)[0]}");
                        }

                        liftedResult = Result<uint[], global::RlWorldWorld.wit.imports.local.rl.IEnv.ErrKind>.err(lifted);
                        break;
                    }

                    default: throw new ArgumentException($"invalid discriminant: {new Span<byte>((void*)(ptr + 0), 1)[0]}");
                }
                if (liftedResult.IsOk) {
                    var tmp = liftedResult.AsOk;
                    return tmp;
                } else {
                    throw new WitException(liftedResult.AsErr!, 0);
                }
            }

            //TODO: free alloc handle (interopString) if exists
        }

    }
}
