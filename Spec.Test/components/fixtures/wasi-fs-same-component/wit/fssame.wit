package local:fssame;
world demo {
    import wasi:filesystem/types@0.2.8;
    /// Calls is-same-object(self, other), returns the bool as
    /// u32 (0 or 1).
    export ask-same: func(self: u32, other: u32) -> u32;
}
