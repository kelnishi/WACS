package local:fieldsfromlist;
world demo {
    import wasi:http/types@0.2.8;
    /// Calls fields.from-list with two entries:
    ///   ("content-type", "text/plain")
    ///   ("authorization", "Bearer XYZ")
    /// Returns the resulting fields handle.
    export ask-from-list: func() -> u32;
}
