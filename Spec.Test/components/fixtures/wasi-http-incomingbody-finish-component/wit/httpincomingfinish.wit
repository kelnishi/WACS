package local:httpincomingfinish;
world demo {
    import wasi:http/types@0.2.3;
    /// Calls [static]incoming-body.finish(handle); returns
    /// the future-trailers handle.
    export ask-finish: func(body: u32) -> u32;
}
