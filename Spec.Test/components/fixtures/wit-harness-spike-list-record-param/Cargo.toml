[package]
name = "wit-harness-spike-list-record-param"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
wit-bindgen = "0.41"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
strip = true
panic = "abort"
lto = true
