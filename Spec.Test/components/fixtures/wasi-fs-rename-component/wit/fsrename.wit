package local:fsrename;
world demo {
    import wasi:filesystem/types@0.2.8;
    /// Calls rename-at(self, "src", new-desc, "dst"); returns
    /// the outer disc byte (always-Ok = 0).
    export ask-rename: func(self: u32, newdesc: u32) -> u32;
}
