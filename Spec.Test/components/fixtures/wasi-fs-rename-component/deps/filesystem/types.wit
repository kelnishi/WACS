package wasi:filesystem@0.2.8;
interface types {
    enum error-code { unknown }
    resource descriptor {
        rename-at: func(
            old-path: string,
            new-descriptor: borrow<descriptor>,
            new-path: string,
        ) -> result<_, error-code>;
    }
}
