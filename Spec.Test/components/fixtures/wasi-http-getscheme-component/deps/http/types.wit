package wasi:http@0.2.8;
interface types {
    variant scheme {
        %HTTP,
        %HTTPS,
        other(string),
    }
    resource outgoing-request {
        scheme: func() -> option<scheme>;
    }
}
