package local:fssymlink;
world demo {
    import wasi:filesystem/types@0.2.8;
    /// Calls symlink-at(handle, "src", "dst"); returns the
    /// outer disc byte (always-Ok = 0).
    export ask-symlink: func(handle: u32) -> u32;
}
