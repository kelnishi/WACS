package local:httperrcode;
world demo {
    import wasi:http/types@0.2.3;
    /// Calls http-error-code(borrow<error-handle>); returns
    /// the outer option disc (0=None, 1=Some).
    export ask-http-error-code: func(err: u32) -> u32;
}
